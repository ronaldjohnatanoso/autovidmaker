create a new py script, this script needs a command line arg, its a name to a project file , like ocean_debate, the script finds that folder by, going up one level to this script, then finds the folder named 0-project-files , then finds the folder named ocean_debate, inside it , it looks for text file named similarly to the project named ,ex: ocean_debate_raw.txt , dont forget that you need the _raw, 